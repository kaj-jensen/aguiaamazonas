---
import type { Tour } from '../data/tours';
interface Props { tour: Tour; lang?: 'en' | 'pt-BR'; featured?: boolean }
const { tour, lang = 'en', featured = false } = Astro.props;
const isPt = lang === 'pt-BR';
const href = isPt ? `/pt/experiencias/${tour.ptSlug}/` : `/experiences/${tour.slug}/`;
---
<article class:list={['tour-card', { featured }]} style={`--accent:${tour.accent}`}>
  <a href={href} class="tour-image" aria-label={isPt ? tour.ptName : tour.name}>
    <img src={tour.image} alt={isPt ? tour.ptName : tour.name} loading="lazy" />
    <span>{tour.duration}</span>
  </a>
  <div class="tour-copy">
    <p class="eyebrow">{tour.eyebrow}</p>
    <h3><a href={href}>{isPt ? tour.ptName : tour.name}</a></h3>
    <p>{isPt ? tour.ptSummary : tour.summary}</p>
    <div class="tour-meta"><span>{tour.group}</span><a href={href}>{isPt ? 'Descobrir' : 'Explore'} <b>↗</b></a></div>
  </div>
</article>
